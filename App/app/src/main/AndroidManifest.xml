<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.estimote.indoorapp">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <application
        android:name="com.example.app.IndoorApplication"
        android:allowBackup="true"
        android:icon="@drawable/beacon_blueberry_small"
        android:label="@string/app_name"
        android:roundIcon="@drawable/beacon_blueberry_small"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.app.MainActivity">
        </activity>
        <activity
            android:name="com.example.app.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:theme="@style/FullscreenTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="com.example.app.LocationListActivity">
        </activity>
    </application>

</manifest>